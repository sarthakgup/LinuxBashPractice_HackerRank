#Given integer inputs X and Y... (first line input X, 2nd is Y)
#Find their sum, diff, product, and quotient

#First reading in both lines of input into X and Y
read X
read Y

#echoing/printing results
echo $((X+Y))
echo $((X-Y))
echo $((X*Y))
echo $((X/Y))
